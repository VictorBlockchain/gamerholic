import type { AuthType } from "../types.js";
export type UseOAuthVerifyReturnType = {
    authenticate: () => void;
    isPending: boolean;
};
export declare const useOAuthVerify: ({ config, }: {
    config: Extract<AuthType, {
        type: "social";
    }>;
}) => UseOAuthVerifyReturnType;
