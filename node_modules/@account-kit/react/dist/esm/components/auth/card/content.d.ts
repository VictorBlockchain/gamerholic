import type { ReactNode } from "react";
interface CardContentProps {
    header: ReactNode | string;
    icon?: ReactNode;
    description: ReactNode | string;
    error?: Error | string;
    className?: string;
    secondaryButton?: {
        title: string;
        onClick: () => void;
    };
}
export declare const CardContent: ({ header, icon, description, secondaryButton, className, }: CardContentProps) => import("react/jsx-runtime").JSX.Element;
export {};
