type ConnectionErrorProps = {
    headerText: string;
    bodyText: string;
    tryAgainCTA?: string;
    icon: React.ReactNode;
    handleTryAgain?: () => void;
    handleUseAnotherMethod?: () => void;
    shouldDisconnect?: boolean;
    handleSkip?: () => void;
};
export declare const ConnectionError: ({ headerText, bodyText, tryAgainCTA, icon, handleTryAgain, handleUseAnotherMethod, shouldDisconnect, handleSkip, }: ConnectionErrorProps) => import("react/jsx-runtime").JSX.Element;
export {};
