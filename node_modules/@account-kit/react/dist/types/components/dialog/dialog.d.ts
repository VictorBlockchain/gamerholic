import { type ReactNode } from "react";
type DialogProps = {
    isOpen: boolean;
    onClose: () => void;
    children: ReactNode;
};
/**
 * Dialog component that renders a modal dialog.
 *
 * @param {DialogProps} props - The props for the Dialog component.
 * @returns {JSX.Element | null} The rendered Dialog component.
 */
export declare const Dialog: ({ isOpen, onClose, children }: DialogProps) => import("react").ReactPortal | null;
export {};
//# sourceMappingURL=dialog.d.ts.map