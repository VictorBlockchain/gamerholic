"use client";

import { TeamsHub } from "@/components/teams/teams-hub";

/**
 * Teams list — create, invite with win split, manage roster.
 * Detail lives at /teams/[id].
 */
export default function TeamsPage() {
  return <TeamsHub />;
}
