"use client";

import { ModeratorConsoleView } from "@/components/moderator/moderator-console-view";

/**
 * Full admin / moderator console — roles, disputes, penalties.
 * Legacy: /moderator/console
 */
export default function ModeratorConsolePage() {
  return <ModeratorConsoleView />;
}
