import { Inter, Exo_2, Sora, Electrolize } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})

export const exo2 = Exo_2({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-exo2',
})

export const sora = Sora({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-sora',
})

export const electrolize = Electrolize({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-electrolize',
})